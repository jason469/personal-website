type tagData = {
    color: string
}

type employmentData = {
    role: string,
    company: string,
    startDate: string,
    endDate: string,
    description: string,
    category: tagData,
    logoName: string,
}